/**
 * Terminal Module - Barrel Export
 */

export { TerminalView, TerminalPlaceholder } from './TerminalView';

// Re-export existing components that are still valid
export { TerminalSearchBar } from '../TerminalSearchBar';
export { TerminalContextMenu } from '../TerminalContextMenu';
