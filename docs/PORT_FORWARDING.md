# OxideTerm 端口转发功能文档

> 本文档描述端口转发的功能、配置和使用方法。

## 目录

1. [功能概述](#功能概述)
2. [转发类型](#转发类型)
3. [界面说明](#界面说明)
4. [使用示例](#使用示例)
5. [API 参考](#api-参考)
6. [故障排除](#故障排除)

---

## 功能概述

OxideTerm 支持三种 SSH 端口转发类型：

| 类型 | SSH 参数 | 用途 |
|------|----------|------|
| 本地转发 | `-L` | 通过 SSH 隧道访问远程服务 |
| 远程转发 | `-R` | 让远程服务器访问本地服务 |
| 动态转发 | `-D` | SOCKS5 代理 |

---

## 转发类型

### 本地转发 (Local Forwarding)

**场景**: 访问远程服务器上只允许本地访问的服务

```
本地机器                  SSH 服务器              目标服务
┌─────────┐              ┌─────────┐            ┌─────────┐
│ Browser │ ──8080───►   │ tunnel  │ ──────►   │ :3306   │
│         │    本地端口   │         │   内网     │ MySQL   │
└─────────┘              └─────────┘            └─────────┘
```

**等效命令**:
```bash
ssh -L 8080:localhost:3306 user@server
```

**配置**:
- **本地绑定**: `localhost:8080`
- **目标地址**: `localhost:3306`

### 远程转发 (Remote Forwarding)

**场景**: 让远程服务器访问本地开发服务

```
本地机器                  SSH 服务器              远程用户
┌─────────┐              ┌─────────┐            ┌─────────┐
│ :3000   │ ◄───────────│ :8080   │ ◄──────────│ Browser │
│ Dev App │              │ 远程端口 │            │         │
└─────────┘              └─────────┘            └─────────┘
```

**等效命令**:
```bash
ssh -R 8080:localhost:3000 user@server
```

**配置**:
- **远程绑定**: `0.0.0.0:8080`
- **目标地址**: `localhost:3000`

### 动态转发 (Dynamic Forwarding / SOCKS5)

**场景**: 将本地端口作为 SOCKS5 代理

```
本地机器                  SSH 服务器              目标网站
┌─────────┐              ┌─────────┐            ┌─────────┐
│ Browser │ ──1080───►   │ proxy   │ ──────►   │ :443    │
│ SOCKS5  │              │         │            │ HTTPS   │
└─────────┘              └─────────┘            └─────────┘
```

**等效命令**:
```bash
ssh -D 1080 user@server
```

**配置**:
- **本地绑定**: `localhost:1080`
- **目标**: (无需配置，动态路由)

---

## 界面说明

### 转发管理面板

```
┌─────────────────────────────────────────────────────────────┐
│ Port Forwards - user@server                    [+ 添加规则] │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐│
│ │ 🟢 本地转发                                              ││
│ │ localhost:8080 → localhost:3306                         ││
│ │ MySQL 数据库                                             ││
│ │ ────────────────────────────────────────────────────────││
│ │ 连接数: 5 │ 流量: ↑1.2MB ↓4.5MB │ 运行: 2h 15m          ││
│ │                          [停止] [编辑] [删除]            ││
│ └─────────────────────────────────────────────────────────┘│
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐│
│ │ 🟢 动态转发 (SOCKS5)                                     ││
│ │ localhost:1080                                          ││
│ │ VPN 代理                                                 ││
│ │ ────────────────────────────────────────────────────────││
│ │ 连接数: 12 │ 流量: ↑15.3MB ↓128.7MB │ 运行: 45m         ││
│ │                          [停止] [编辑] [删除]            ││
│ └─────────────────────────────────────────────────────────┘│
│                                                             │
│ ─────────────────── 已停止的规则 ────────────────────────── │
│                                                             │
│ ┌─────────────────────────────────────────────────────────┐│
│ │ ⚫ 远程转发                                              ││
│ │ 0.0.0.0:8080 → localhost:3000                           ││
│ │                           [重启] [编辑] [删除]           ││
│ └─────────────────────────────────────────────────────────┘│
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 状态指示

| 图标 | 状态 | 说明 |
|------|------|------|
| 🟢 | 活跃 | 转发正在运行 |
| 🟡 | 启动中 | 正在建立转发 |
| 🔴 | 错误 | 转发失败 |
| ⚫ | 已停止 | 转发已停止 |

### 添加规则对话框

```
┌─────────────────────────────────────────────┐
│ 添加端口转发规则                         ✕  │
├─────────────────────────────────────────────┤
│                                             │
│ 转发类型                                    │
│ ○ 本地转发  ○ 远程转发  ○ 动态转发          │
│                                             │
│ [本地/远程转发时显示]                       │
│ ─────────────────────────────────────────── │
│ 绑定地址              绑定端口              │
│ ┌─────────────────┐  ┌───────────────┐     │
│ │ localhost       │  │ 8080          │     │
│ └─────────────────┘  └───────────────┘     │
│                                             │
│ 目标主机              目标端口              │
│ ┌─────────────────┐  ┌───────────────┐     │
│ │ localhost       │  │ 3306          │     │
│ └─────────────────┘  └───────────────┘     │
│                                             │
│ [动态转发时显示]                            │
│ ─────────────────────────────────────────── │
│ 绑定地址              绑定端口              │
│ ┌─────────────────┐  ┌───────────────┐     │
│ │ localhost       │  │ 1080          │     │
│ └─────────────────┘  └───────────────┘     │
│                                             │
│ 描述 (可选)                                 │
│ ┌───────────────────────────────────────┐  │
│ │ MySQL 数据库连接                      │  │
│ └───────────────────────────────────────┘  │
│                                             │
│              [取消]     [创建]              │
└─────────────────────────────────────────────┘
```

---

## 使用示例

### 示例 1: 访问远程 MySQL

**场景**: 远程服务器有 MySQL，只允许 localhost 连接

1. 创建本地转发:
   - 类型: `本地转发`
   - 绑定: `localhost:3306`
   - 目标: `localhost:3306`

2. 本地连接:
   ```bash
   mysql -h 127.0.0.1 -P 3306 -u root -p
   ```

### 示例 2: 访问远程 Web 应用

**场景**: 远程服务器有 Web 应用在 8080 端口

1. 创建本地转发:
   - 类型: `本地转发`
   - 绑定: `localhost:8080`
   - 目标: `localhost:8080`

2. 浏览器访问: `http://localhost:8080`

### 示例 3: 暴露本地开发服务器

**场景**: 让同事访问你本地的开发服务器

1. 创建远程转发:
   - 类型: `远程转发`
   - 绑定: `0.0.0.0:8080` (允许外部访问)
   - 目标: `localhost:3000`

2. 同事访问: `http://your-server:8080`

> ⚠️ 注意: 需要 SSH 服务器配置 `GatewayPorts yes`

### 示例 4: 科学上网

**场景**: 使用 SSH 服务器作为代理

1. 创建动态转发:
   - 类型: `动态转发`
   - 绑定: `localhost:1080`

2. 配置浏览器 SOCKS5 代理:
   - 代理服务器: `127.0.0.1`
   - 端口: `1080`

---

## API 参考

### 创建转发

```typescript
interface ForwardRequest {
  session_id: string;
  forward_type: 'local' | 'remote' | 'dynamic';
  bind_address: string;  // 如 "localhost" 或 "0.0.0.0"
  bind_port: number;
  target_host: string;   // 本地/远程转发需要
  target_port: number;   // 本地/远程转发需要
  description?: string;
}

// 创建转发规则
const ruleId = await api.createPortForward(request);
```

### 管理转发

```typescript
// 获取所有转发规则
const rules: ForwardRule[] = await api.listPortForwards(sessionId);

// 停止转发 (不删除，可重启)
await api.stopPortForward(sessionId, ruleId);

// 重启已停止的转发
await api.restartPortForward(sessionId, ruleId);

// 删除转发规则
await api.deletePortForward(sessionId, ruleId);

// 更新转发规则
await api.updatePortForward(sessionId, ruleId, {
  description: "新描述"
});
```

### 获取统计

```typescript
interface ForwardStats {
  rule_id: string;
  active_connections: number;
  total_connections: number;
  bytes_sent: number;
  bytes_received: number;
  started_at: number;  // Unix timestamp
}

// 获取单个规则统计
const stats: ForwardStats = await api.getPortForwardStats(sessionId, ruleId);

// 获取所有规则统计
const allStats: ForwardStats[] = await api.getAllPortForwardStats(sessionId);
```

### ForwardRule 类型

```typescript
interface ForwardRule {
  id: string;
  forward_type: 'local' | 'remote' | 'dynamic';
  bind_address: string;
  bind_port: number;
  target_host: string;
  target_port: number;
  status: 'starting' | 'active' | 'stopped' | 'error';
  description?: string;
  error?: string;
}
```

---

## 故障排除

### 常见错误

#### "Address already in use"

**原因**: 端口已被占用

**解决**:
```bash
# 查看端口占用
lsof -i :8080
# 或
netstat -an | grep 8080

# 终止占用进程或使用其他端口
```

#### "Permission denied"

**原因**: 绑定特权端口 (<1024)

**解决**: 使用 1024 以上的端口，或以 root 权限运行

#### "Connection refused"

**原因**: 目标服务未运行或拒绝连接

**解决**: 确认目标服务正在运行且监听正确端口

#### 远程转发不工作

**原因**: SSH 服务器配置不允许

**解决**: 检查服务器 `/etc/ssh/sshd_config`:
```
GatewayPorts yes      # 允许绑定到非 localhost
AllowTcpForwarding yes
```

### 调试技巧

1. **查看连接状态**
   ```bash
   # 查看 TCP 连接
   netstat -an | grep ESTABLISHED
   ```

2. **测试端口连通性**
   ```bash
   nc -zv localhost 8080
   ```

3. **查看应用日志**
   ```bash
   RUST_LOG=debug npm run tauri dev
   ```

---

## 安全建议

1. **避免绑定 0.0.0.0**
   - 除非必要，否则只绑定 `localhost`
   - 绑定 `0.0.0.0` 会暴露服务给网络

2. **使用强认证**
   - 使用 SSH 密钥而非密码
   - 定期轮换密钥

3. **限制转发范围**
   - 只转发必要的端口
   - 使用完毕后及时停止

4. **监控连接**
   - 定期检查活跃连接
   - 关注异常流量
